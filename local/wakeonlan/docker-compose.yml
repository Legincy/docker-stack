services:
  wakeonlan:
    image: alpine
    container_name: wakeonlan
    restart: "no"
    network_mode: host
    command: >
      sh -c "apk add --no-cache wakeonlan &&
             wakeonlan ${TARGET_MAC} &&
             sleep 5"